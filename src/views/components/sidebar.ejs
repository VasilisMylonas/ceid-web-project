<nav class="sidebar z-3 position-fixed top-0 start-0 border-end">
  <div class="container-fluid h-100 d-flex flex-column p-0">

    <div class="row border-bottom">
      <div class="col-12">
        <div class="sidebar-header mx-2 d-flex flex-row-reverse justify-content-between align-items-center flex-md-column justify-content-md-center align-items-md-center">
          <button class="btn" id="sidebar-toggle" aria-label="Toggle sidebar">
            <i class="bi bi-list" aria-hidden="true"></i>
          </button>
          <img src="/images/upatras_logo.gif" alt="Πανεπιστήμιο Πατρών" class="img-fluid p-2" />
        </div>
      </div>
    </div>

    <div class="row flex-grow-1">
      <div class="col-12">
        <ul class="sidebar-menu nav flex-column">
          <% links.forEach((link) => { %>
          <!-- Prefetch  for performance -->
          <link rel="prefetch" href="<%= link.href %>" />
          <li class="nav-item">
            <a href="<%= link.href %>" class="nav-link p-3 d-flex align-items-center <%= page === link.href ? 'active' : ''%>">
              <i class="bi <%= link.icon %> nav-link-icon" aria-hidden="true"></i>
              <span class="nav-link-text ms-3">
                <%= link.title %>
              </span>
            </a>
          </li>
          <% }); %>
        </ul>
      </div>
    </div>

    <div class="row border-top">
      <div class="col-md-12">
        <ul class="sidebar-footer nav flex-column">
          <li class="nav-item">
            <a href="/" class="nav-link p-3 d-flex align-items-center">
              <i class="bi bi-gear-fill nav-link-icon" aria-hidden="true"></i>
              <span class="nav-link-text ms-3">Ρυθμίσεις</span>
            </a>
          </li>
          <li class="nav-item">
            <form id="logoutForm" action="/logout" method="POST">
              <button type="submit" class="btn btn-link nav-link p-3 d-flex align-items-center w-100 rounded-0">
                <i class="bi bi-door-open nav-link-icon" aria-hidden="true"></i>
                <span class="nav-link-text ms-3">Αποσύνδεση</span>
              </button>
            </form>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
